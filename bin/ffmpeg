#!/usr/bin/env bash

sys="$(uname)"
exe=/usr/bin/ffmpeg

if [ "$sys" = Darwin ]; then
  exe=/opt/homebrew/bin/ffmpeg
fi

# -y ... always override
# --preset slow ... increases encoding time but
#                   improves output quality and
#                   reduces file size
"$exe" --preset slow -y "$@"

